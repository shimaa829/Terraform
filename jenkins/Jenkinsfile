pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'terraform init'
                sh 'terraform apply -var-file=dev.tfvars'

            }
           }
        }
    }
